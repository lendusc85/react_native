import { TextInput, View } from 'react-native';

const GameOverScreen = () => {
    return(
        <View>
            <TextInput />
            {/* Buttons */}
        </View>
    )
};

export default GameOverScreen;